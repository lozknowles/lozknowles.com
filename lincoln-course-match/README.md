# Course Match production mirror

This directory mirrors the public static source for Course Match 1.0.0.

Canonical repository: https://github.com/lozknowles/course-matcher

Runtime OCR/PDF vendor assets are generated from the canonical repository with `npm install && npm run vendor` and deployed by `scripts/deploy-course-matcher.sh`.
